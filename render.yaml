services:
  - type: web
    name: sentiment-nlp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c "uvicorn src.serving.app:app --host 0.0.0.0 --port ${PORT:-10000}"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    plan: free

